
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции

// Получение списка товаров по параметрам.
//
// Параметры:
//  Наименование - Строка	- Представление корзины.
//  Дата		 - Дата		- Дата формирования корзины.
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Список номенклатуры.
//
Функция ТоварыИзКорзины(Наименование, Дата) Экспорт
	
	МенеджерЗаписи = РегистрыСведений.Корзина.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Дата         = Дата;
	МенеджерЗаписи.Наименование = Наименование;
	МенеджерЗаписи.Прочитать();
	
	Если МенеджерЗаписи.Выбран() Тогда
		Товары = ПолучитьЗначениеПараметраСтруктуры(МенеджерЗаписи.Значение.Получить(), "Товары", Новый ТаблицаЗначений);
	Иначе
		Товары = Новый ТаблицаЗначений;
	КонецЕсли;
	
	Возврат Товары;
	
КонецФункции

#Область ПараметрыОбработкиРеквизитовОбъекта

Функция ПолучитьОбязательныеРеквизиты(Объект) Экспорт
	
	
	ОбязательныеРеквизиты = ОбработкаСобытийРегистраСервер.ПолучитьСтандартныеОбязательныеРеквизиты(Объект);
	
	Возврат ОбязательныеРеквизиты;
	
КонецФункции 

Функция ПолучитьУникальныеРеквизиты(Объект) Экспорт
	
	УникальныеРеквизиты = Новый Структура();
	
	Возврат УникальныеРеквизиты;
	
КонецФункции 

#КонецОбласти

#КонецОбласти

#КонецЕсли