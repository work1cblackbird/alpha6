#Область ОбработчикиСобытий

&НаКлиенте
Асинх Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ПараметрыАнкеты = ПолучитьПараметры(ПараметрКоманды);
	АнкетированиеАльфаАвтоКлиент.НачатьИнтервью(ПараметрыАнкеты);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ПолучитьПараметры(Событие)
	
	Результат = Новый Структура("Респондент, ДокументОснование");
	
	Результат.Респондент        = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Событие, "Контрагент");
	Результат.ДокументОснование = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Событие, "Ссылка");
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти
