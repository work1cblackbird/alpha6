#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Распределить(Команда)
	
	Если ТаможеннаяСтоимость ИЛИ СуммаПошлины ИЛИ СуммаНДС Тогда
		
		Распределить = Новый Структура();
		Распределить.Вставить("ТаможеннаяСтоимость", ТаможеннаяСтоимость);
		Распределить.Вставить("СуммаПошлины", СуммаПошлины);
		Распределить.Вставить("СуммаНДС", СуммаНДС);
		
	КонецЕсли;
	
	Закрыть(Распределить);
	
КонецПроцедуры

#КонецОбласти