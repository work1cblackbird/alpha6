#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Обработчик при изменении настроек.
//
// Параметры:
//  Форма - УправляемаяФорма - Форма, в которой возникло событие.
//
Процедура ПриИзмененииНастроек(Форма) Экспорт
	
	ПараметрыДанных = КомпоновщикНастроек.Настройки.ПараметрыДанных;
	
	СценарийПланированияПоле     = Новый ПараметрКомпоновкиДанных("СценарийПланирования");
	СценарийПланированияПараметр = ПараметрыДанных.НайтиЗначениеПараметра(СценарийПланированияПоле);
	
	СценарийПланирования = СценарийПланированияПараметр.Значение;
	
	ВыводитьНезапланированныеДанныеПоле     = Новый ПараметрКомпоновкиДанных("ВыводитьНезапланированныеДанные");
	ВыводитьНезапланированныеДанныеПараметр = ПараметрыДанных.НайтиЗначениеПараметра(ВыводитьНезапланированныеДанныеПоле);
	
	ВыводитьНезапланированныеДанные = ВыводитьНезапланированныеДанныеПараметр.Значение;
	
	Форма.ЗначениеВРеквизитФормы(ЭтотОбъект, "Отчет");
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	ОтчетыПлатформаСервер.ВывестиОтчет(ЭтотОбъект, ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка);
	
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	Параметры = КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы;
	ПараметрыСКД = СхемаКомпоновкиДанных.Параметры;
	
	ОтчетыПлатформаСервер.ПроверкаЗаполненияПараметровОтчета(Параметры, ПараметрыСКД, Отказ);
КонецПроцедуры

#КонецОбласти

#КонецЕсли