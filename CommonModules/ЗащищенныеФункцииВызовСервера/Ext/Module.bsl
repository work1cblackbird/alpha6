/////////////////////////////////////////////////////////////////////////////////////////////////////
// ЗАЩИЩЕННЫЕ ФУНКЦИИ СЕРВЕРНОГО КОНТЕКСТА
/////////////////////////////////////////////////////////////////////////////////////////////////////


#Область СлужебныеПроцедурыИФункции

// Процедура производит получение представления расширенных итогов видимости колонки Характеристика
// в зависимости от значения служебных реквизитов.
//
// Параметры:
//  Объект - ДанныеФормыСтруктура - Объект, для которого выполняется формирование представления итогов.
// 
// Возвращаемое значение:
//  Структура
//
Функция ПолучитьПредставленияИтоговОперации(Знач Объект) Экспорт
	
	Возврат ЗащищенныеФункцииСервер.ПолучитьПредставленияИтоговОперации(Объект);
	
КонецФункции

// Возвращает вид счета-фактуры для переданного документа.
//
// Параметры:
//  Документ - ДокументСсылка - Ссылка на документ, для которого необходимо получить вид счета-фактуры
//
// Возвращаемое значение:
//  Строка, Неопределено - Вид счета-фактуры(СчетФактураВыданный, СчетФактураПолученный),
//                         либо неопределенное значение, для документов, которым не выписывается счет-фактура.
//
Функция ПолучитьВидПодчиненнойСчетаФактуры(Документ) Экспорт
	
	Возврат ЗащищенныеФункцииСервер.ПолучитьВидПодчиненнойСчетаФактуры(Документ);
	
КонецФункции

#КонецОбласти