////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// В данном модуле располагаются обработчики расширений событий форм в клиентском контексте.
//
// Правила внедрения методов расширений в объекты см. СобытияОбъектовФормы
// 
// Вызов методов расширений следует завернуть в комментарий с именем подсистемы.
//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// При открытии форма списка.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения -
//  Отказ - Булево -
Процедура ПриОткрытии_ФормаСписка(Форма, Отказ) Экспорт
	
	МодулиДляОбработки = СобытияОбъектовСлужебныйКлиентСервер.МодулиДляОбработки(Форма.ИмяФормы,
		СобытияОбъектовФормыКлиентПереопределяемый);
	
	Для Каждого Модуль Из МодулиДляОбработки Цикл
		
		Модуль.ПриОткрытии_ФормаСписка(Форма, Отказ);
		
	КонецЦикла;
	
КонецПроцедуры

// Обработка оповещения форма списка.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения -
//  ИмяСобытия - Строка - 
//  Параметр - Произвольный - 
//  Источник - Произвольный -
//
Процедура ОбработкаОповещения_ФормаСписка(Форма, ИмяСобытия, Параметр, Источник) Экспорт
	
	МодулиДляОбработки = СобытияОбъектовСлужебныйКлиентСервер.МодулиДляОбработки(Форма.ИмяФормы,
		СобытияОбъектовФормыКлиентПереопределяемый);
	
	Для Каждого Модуль Из МодулиДляОбработки Цикл
		
		Модуль.ОбработкаОповещения_ФормаСписка(Форма, ИмяСобытия, Параметр, Источник);
		
	КонецЦикла;
	
КонецПроцедуры

// При открытии форма документа.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения -
//  Отказ - Булево -
//
Процедура ПриОткрытии_ФормаДокумента(Форма, Отказ) Экспорт
	
	МодулиДляОбработки = СобытияОбъектовСлужебныйКлиентСервер.МодулиДляОбработки(Форма.ИмяФормы,
		СобытияОбъектовФормыКлиентПереопределяемый);
	
	Для Каждого Модуль Из МодулиДляОбработки Цикл
		
		Модуль.ПриОткрытии_ФормаДокумента(Форма, Отказ);
		
	КонецЦикла;
	
КонецПроцедуры

// Обработка оповещения форма документа.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//  ИмяСобытия - Строка -
//  Параметр - Произвольный -
//  Источник - Произвольный -
//
Процедура ОбработкаОповещения_ФормаДокумента(Форма, ИмяСобытия, Параметр, Источник) Экспорт
	
	МодулиДляОбработки = СобытияОбъектовСлужебныйКлиентСервер.МодулиДляОбработки(Форма.ИмяФормы,
		СобытияОбъектовФормыКлиентПереопределяемый);
	
	Для Каждого Модуль Из МодулиДляОбработки Цикл
		
		Модуль.ОбработкаОповещения_ФормаДокумента(Форма, ИмяСобытия, Параметр, Источник);
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти