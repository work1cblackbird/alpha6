
#Область ПрограммныйИнтерфейс

// Список выбор.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - Форма;
//  Элемент - ПолеФормы - Элемент;
//  ВыбраннаяСтрока - ОпределяемыйТип.ПодписываемыеДокументыПЭП - Выбранный документ;
//  Поле - ПолеФормы - Поле;
//  СтандартнаяОбработка - Булево - Стандартная обработка;
Процедура СписокВыбор(Форма, Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка) Экспорт
	
	ЭлементЗаявкиНаПодписьПредставление = Форма.Элементы.Найти("ЗаявкиНаПодписьПредставление");
	
	Если ЭлементЗаявкиНаПодписьПредставление <> Неопределено И Поле = ЭлементЗаявкиНаПодписьПредставление Тогда
		
		СтандартнаяОбработка = Ложь;
		ОткрытьФорму(
			"Документ.ЗаявкаНаПодпись.Форма.ФормаСписка",
			Новый Структура("Отбор", Новый Структура("ДокументОснование", ВыбраннаяСтрока)),
			Форма,
			Форма,
			,
			,
			,
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца
		);
		
	КонецЕсли;
	
КонецПроцедуры

// Заявки на подпись представление нажатие форма документа.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - Форма документа учета;
Процедура ЗаявкиНаПодписьПредставлениеНажатие_ФормаДокумента(Форма) Экспорт
	
	ОткрытьФорму(
		"Документ.ЗаявкаНаПодпись.Форма.ФормаСписка",
		Новый Структура("Отбор", Новый Структура("ДокументОснование", Форма.Объект.Ссылка)),
		Форма,
		Форма,
		,
		,
		,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца
	);
	
КонецПроцедуры

#КонецОбласти
