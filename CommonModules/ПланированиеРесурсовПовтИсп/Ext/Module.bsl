
#Область СлужебныйПрограммныйИнтерфейс

// Оформление графиков.
// 
// Возвращаемое значение:
//  Структура - Полученное оформление графиков из макета.
//
Функция ОформлениеГрафиков() Экспорт
	
	Результат = Новый Структура;
	МакетОформления   = Справочники.ГрафикиРаботы.ПолучитьМакет("МакетОформления");
	ОбластьОформления = МакетОформления.ПолучитьОбласть("Оформление");
	
	Для ПеременнаяЦикла = 1 По ОбластьОформления.ВысотаТаблицы Цикл
		
		ТекущаяОбласть = ОбластьОформления.Область(ПеременнаяЦикла, 1);
		
		Если НЕ ЗначениеЗаполнено(ТекущаяОбласть.Текст) Тогда
			
			Продолжить;
			
		КонецЕсли;
		
		Результат.Вставить("Цвет" + ТекущаяОбласть.Текст, ТекущаяОбласть.ЦветФона);
		
	КонецЦикла;
	
	Результат.Вставить("ЦветВыделенияДокумента", ЦветаСтиля.ЗаписьНаРемонтВыделенныйДокумент);
	Результат.Вставить("ЦветВыделенияРаботы"   , ЦветаСтиля.ЗаписьНаРемонтВыделеннаяРабота);
	Результат.Вставить("ЦветРамкиСтандарт"     , ЦветаСтиля.ЦветРамки);
	Результат.Вставить("ПересечениеИнтервалов" , ЦветаСтиля.ЗаписьНаРемонтПересечениеИнтервалов);
	Результат.Вставить("ЗанятоДругимДокументом", ЦветаСтиля.ЗаписьНаРемонтЗанятоДругимДокументом);
	Результат.Вставить("ТекущийДокумент"       , ЦветаСтиля.ЗаписьНаРемонтТекущийДокумент);
	Результат.Вставить("ТекущийИнтервал"       , ЦветаСтиля.ЗаписьНаРемонтТекущийИнтервал);
	Результат.Вставить("ШрифтТекста"           , ШрифтыСтиля.ЗаписьНаРемонтШрифтГрафика);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти