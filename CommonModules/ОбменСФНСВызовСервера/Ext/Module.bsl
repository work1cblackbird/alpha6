
#Область ПрограммныйИнтерфейс

// Получение параметров для создания длительной операции проверки РНПТ.
//
// Параметры:
//  УникальныйИдентификатор	 - УникальныйИдентификатор - Идентификатор фонового задания.
//  ВходныеПараметры		 - Структура - Параметры для проверки РНПТ:
//    * РНПТ - СправочникСсылка.ГТД - РНПТ для проверки ФНС;
//    * Организация - СправочникСсылка.Организации - Организация для определения настроек подключения к ФНС.
// 
// Возвращаемое значение:
//  Структура - см. функцию ДлительныеОперации.ВыполнитьВФоне();
//
Функция ЗапросПроверкиРНПТ(УникальныйИдентификатор, ВходныеПараметры) Экспорт
	
	// Переопределим параметры запроса
	ОбменСФНСПереопределяемый.ЗаполнитьПараметрыПроверкиРНПТ(ВходныеПараметры);
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания =
		НСтр("ru = 'Проверка регистрационного номера партии товара (РНПТ)'");
	
	ДлительнаяОперация = ДлительныеОперации.ВыполнитьВФоне(
		"ОбменСФНССервер.ВыполнитьЗапросПроверкиРНПТ",
		ВходныеПараметры,
		ПараметрыВыполнения);
	
	Возврат ДлительнаяОперация;
	
КонецФункции

#КонецОбласти
