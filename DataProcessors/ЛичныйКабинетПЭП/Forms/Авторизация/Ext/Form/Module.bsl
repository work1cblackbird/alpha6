
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Авторизация(Команда)
	
	Токен = ВыполнитьАвторизацию(Пользователь, Пароль);
	
	Если ЗначениеЗаполнено(Токен) Тогда
		
		Закрыть(Токен);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервереБезКонтекста
Функция ВыполнитьАвторизацию(Пользователь, Пароль)
	
	Возврат ОбщегоНазначенияПЭП.МодульИнтеграции().Авторизация(Пользователь, Пароль);
	
КонецФункции

#КонецОбласти