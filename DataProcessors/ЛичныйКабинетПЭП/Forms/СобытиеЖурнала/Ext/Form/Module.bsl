
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЗаполнитьЗначенияСвойств(
		ЭтотОбъект,
		Параметры,
		"ДатаСоздания,ИдентификаторДанных,ИдентификаторЗаписи,ПодсистемаПЭП,УровеньЖурнала"
	);
	
	Если Параметры.Контекст <> Неопределено Тогда
		
		Контекст = HTTPТранспортПЭПВызовСервера.СобратьJSON(Параметры.Контекст);
		
	КонецЕсли;
	
	Если Параметры.Данные <> Неопределено Тогда
		
		Данные = HTTPТранспортПЭПВызовСервера.СобратьJSON(Параметры.Данные);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
