// Модуль менеджера документа "Поступление товаров"

#Область СлужебныеПроцедурыИФункции

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	// Получим значения функциональных опций
	ИспользоватьОрдерныйСклад  = ПолучитьФункциональнуюОпцию("ИспользоватьОрдерныйСклад");
	ИспользоватьЯчейкиХранения = ПолучитьФункциональнуюОпцию("ИспользоватьЯчейкиХранения");
	
	// Ограничения на список не накладываются
	Если ИспользоватьОрдерныйСклад И ИспользоватьЯчейкиХранения Тогда
		Возврат;
	КонецЕсли;
	
	СтандартнаяОбработка = ЛОЖЬ;
	ДанныеВыбора         = Новый СписокЗначений();
	
	Для каждого Значение Из Метаданные.Перечисления.ВидыСкладов.ЗначенияПеречисления Цикл
		
		Если Значение.Имя="Ордерный" И (НЕ ИспользоватьОрдерныйСклад) Тогда
			Продолжить;
		ИначеЕсли Значение.Имя="Ячеистый" И (НЕ ИспользоватьЯчейкиХранения) Тогда
			Продолжить;
		ИначеЕсли Значение.Имя="ОрдерныйЯчеистый" И НЕ (ИспользоватьЯчейкиХранения И ИспользоватьОрдерныйСклад) Тогда
			Продолжить;
		ИначеЕсли ЗначениеЗаполнено(СокрЛП(Параметры.СтрокаПоиска)) И (НЕ СтрНайти(ВРег(Значение.Синоним), ВРег(Параметры.СтрокаПоиска))) Тогда
			Продолжить;
		КонецЕсли;
		
		ДанныеВыбора.Добавить(Перечисления.ВидыСкладов[Значение.Имя], Значение.Синоним);
		
	КонецЦикла;
	
КонецПроцедуры // ОбработкаПолученияДанныхВыбора()

#КонецОбласти